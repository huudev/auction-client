query getUserById($id: ID!){
  user(id: $id){
    id
    userName
    createTime
    amount
    firstName
    lastName
    address
    birthday
    phoneNumber
    vipMember
    email
    activeStatus
    role
    products {
      ownerId
      createTime
    }
  }
}
